<?xml version="1.0" encoding="utf-8"?>
<!--
  This file is the technical backbone of RedForge's privacy promise.
  We deliberately exclude ALL app data (database, datastore, photos)
  from cloud backup and device-to-device transfer, so a user's workout
  history and progress photos genuinely never leave the device.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapability="true">
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
